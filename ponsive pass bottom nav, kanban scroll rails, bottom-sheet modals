warning: in the working copy of 'src/App.jsx', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/src/App.jsx b/src/App.jsx[m
[1mindex f77dd51..3672f6d 100644[m
[1m--- a/src/App.jsx[m
[1m+++ b/src/App.jsx[m
[36m@@ -117,6 +117,18 @@[m [mfunction useGlobalStyles() {[m
       @keyframes pulse { 0%,100% { opacity: 1; } 50% { opacity: 0.45; } }[m
       @keyframes fadein { from { opacity: 0; transform: translateY(3px); } to { opacity: 1; transform: none; } }[m
       @keyframes spin { to { transform: rotate(360deg); } }[m
[32m+[m[32m      @keyframes slideup { from { opacity: 0; transform: translateY(28px); } to { opacity: 1; transform: none; } }[m
[32m+[m[32m      html { overflow-x: hidden; }[m
[32m+[m[32m      body { overflow-x: hidden; }[m
[32m+[m[32m      @media (hover: hover) and (pointer: fine) {[m
[32m+[m[32m        .zts-card-hover:hover { transform: translateY(-2px); box-shadow: 0 4px 8px rgba(15,23,42,0.06), 0 16px 40px rgba(15,23,42,0.08); }[m
[32m+[m[32m      }[m
[32m+[m[32m      @media (max-width: ${MOBILE_BP}px) {[m
[32m+[m[32m        input, select, textarea { font-size: 16px !important; }[m
[32m+[m[32m      }[m
[32m+[m[32m      @media (prefers-reduced-motion: reduce) {[m
[32m+[m[32m        *, *::before, *::after { animation-duration: 0.01ms !important; animation-iteration-count: 1 !important; transition-duration: 0.01ms !important; }[m
[32m+[m[32m      }[m
     `;[m
     document.head.appendChild(style);[m
   }, []);[m
[36m@@ -135,16 +147,81 @@[m [mconst syne = "'Syne', system-ui";[m
 const mono = "'DM Mono', monospace";[m
 [m
 const Card = ({ children, style, onClick, hover }) => ([m
[31m-  <div onClick={onClick}[m
[31m-    onMouseEnter={hover ? (e) => { e.currentTarget.style.transform = "translateY(-2px)"; e.currentTarget.style.boxShadow = "0 4px 8px rgba(15,23,42,0.06), 0 16px 40px rgba(15,23,42,0.08)"; } : undefined}[m
[31m-    onMouseLeave={hover ? (e) => { e.currentTarget.style.transform = "none"; e.currentTarget.style.boxShadow = T.cardShadow; } : undefined}[m
[31m-    style={{ background: T.card, borderRadius: "16px", border: `1px solid ${T.line}`, boxShadow: T.cardShadow, padding: "18px 20px", cursor: onClick ? "pointer" : "default", ...style }}>{children}</div>[m
[32m+[m[32m  <div onClick={onClick} className={hover ? "zts-card-hover" : undefined}[m
[32m+[m[32m    style={{ background: T.card, borderRadius: "16px", border: `1px solid ${T.line}`, boxShadow: T.cardShadow, padding: "18px 20px", cursor: onClick ? "pointer" : "default", transition: hover ? "transform 0.15s ease, box-shadow 0.15s ease" : undefined, ...style }}>{children}</div>[m
 );[m
 const Label = ({ children, style }) => <div style={{ fontSize: "11px", fontWeight: 700, color: T.sub, textTransform: "uppercase", letterSpacing: "0.13em", fontFamily: syne, ...style }}>{children}</div>;[m
 const Btn = ({ children, onClick, primary, disabled, style }) => ([m
   <button onClick={onClick} disabled={disabled}[m
     style={{ padding: "10px 18px", background: disabled ? "rgba(15,23,42,0.06)" : primary ? T.navyGrad : "transparent", border: primary ? "1px solid rgba(245,158,11,0.2)" : `1px solid ${T.line}`, borderRadius: "10px", color: disabled ? T.faint : primary ? "#FFFFFF" : T.sub, fontSize: "12px", fontWeight: 700, cursor: disabled ? "default" : "pointer", fontFamily: syne, letterSpacing: "0.02em", ...style }}>{children}</button>[m
 );[m
[32m+[m
[32m+[m[32m// ─── Mobile retrofit — shared responsive helpers (desktop paths untouched) ───[m
[32m+[m[32m// Single breakpoint: phones only (largest phones ~430px, smallest tablets[m
[32m+[m[32m// ~768px — 680 sits in the gap so tablets/desktop windows are never caught).[m
[32m+[m[32mconst MOBILE_BP = 680;[m
[32m+[m[32mfunction useIsMobile() {[m
[32m+[m[32m  const [isMobile, setIsMobile] = useState(() => typeof window !== "undefined" ? window.innerWidth <= MOBILE_BP : false);[m
[32m+[m[32m  useEffect(() => {[m
[32m+[m[32m    const onResize = () => setIsMobile(window.innerWidth <= MOBILE_BP);[m
[32m+[m[32m    window.addEventListener("resize", onResize);[m
[32m+[m[32m    return () => window.removeEventListener("resize", onResize);[m
[32m+[m[32m  }, []);[m
[32m+[m[32m  return isMobile;[m
[32m+[m[32m}[m
[32m+[m[32mconst viewPad = (isMobile) => (isMobile ? "16px 16px 26px" : "24px 28px");[m
[32m+[m[32m// Kanban boards: unchanged grid on desktop; a horizontal scroll-snap rail on[m
[32m+[m[32m// mobile so every stage stays reachable with a swipe instead of collapsing.[m
[32m+[m[32mconst kanbanWrapStyle = (isMobile, cols) => isMobile[m
[32m+[m[32m  ? { display: "flex", gap: "10px", overflowX: "auto", WebkitOverflowScrolling: "touch", scrollSnapType: "x proximity", paddingBottom: "6px" }[m
[32m+[m[32m  : { display: "grid", gridTemplateColumns: `repeat(${cols}, 1fr)`, gap: "12px", alignItems: "start" };[m
[32m+[m[32mconst kanbanColStyle = (isMobile) => isMobile ? { minWidth: "80vw", maxWidth: "80vw", flexShrink: 0, scrollSnapAlign: "start" } : undefined;[m
[32m+[m
[32m+[m[32m// Shared modal chrome: centered card on desktop (unchanged), bottom sheet on[m
[32m+[m[32m// mobile so it feels native instead of a floating box on a big screen.[m
[32m+[m[32mfunction ModalShell({ onClose, isMobile, width = 560, children }) {[m
[32m+[m[32m  return ([m
[32m+[m[32m    <div onClick={onClose} style={{ position: "fixed", inset: 0, background: "rgba(11,18,32,0.5)", zIndex: 300, display: "flex", alignItems: isMobile ? "flex-end" : "center", justifyContent: "center", animation: `${isMobile ? "slideup" : "fadein"} 0.18s ease both` }}>[m
[32m+[m[32m      <div onClick={e => e.stopPropagation()} style={{ background: T.card, borderRadius: isMobile ? "20px 20px 0 0" : "18px", width: isMobile ? "100%" : `${width}px`, maxWidth: isMobile ? "100%" : "94vw", maxHeight: isMobile ? "90vh" : "88vh", display: "flex", flexDirection: "column", boxShadow: isMobile ? "0 -12px 40px rgba(11,17,32,0.22)" : "0 32px 80px rgba(11,17,32,0.24)", overflow: "hidden" }}>[m
[32m+[m[32m        {isMobile && <div style={{ width: "36px", height: "4px", borderRadius: "3px", background: "rgba(15,23,42,0.16)", margin: "10px auto -4px", flexShrink: 0 }} />}[m
[32m+[m[32m        {children}[m
[32m+[m[32m      </div>[m
[32m+[m[32m    </div>[m
[32m+[m[32m  );[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32m// Bottom-nav icon set — plain geometry only (circles/lines/polygons), no[m
[32m+[m[32m// icon library dependency, matches the app's existing minimal glyph style.[m
[32m+[m[32mconst TAB_ICONS = {[m
[32m+[m[32m  mission: (c) => <><polyline points="4,11 12,4.5 20,11" /><path d="M6 10 V19.5 H18 V10" /></>,[m
[32m+[m[32m  creators: (c) => <><circle cx="8.6" cy="7.8" r="2.5" /><polygon points="8.6,11.3 4.2,19 13,19" /><circle cx="16.6" cy="9.4" r="2.1" /><polygon points="16.6,12.4 13.3,19 19.9,19" /></>,[m
[32m+[m[32m  studio: (c) => <><rect x="3.5" y="5.5" width="17" height="13" rx="2" /><polygon points="10,9.3 10,14.7 15,12" fill={c} stroke="none" /></>,[m
[32m+[m[32m  seo: (c) => <><circle cx="10.3" cy="10.3" r="6" /><line x1="14.7" y1="14.7" x2="20" y2="20" /></>,[m
[32m+[m[32m  agents: (c) => <><circle cx="12" cy="5.5" r="2" /><circle cx="5.8" cy="17" r="2" /><circle cx="18.2" cy="17" r="2" /><line x1="12" y1="7.5" x2="7.2" y2="15.3" /><line x1="12" y1="7.5" x2="16.8" y2="15.3" /></>,[m
[32m+[m[32m  ops: (c) => <polyline points="3,13 7,13 9,19 13,6 15,13 21,13" />,[m
[32m+[m[32m};[m
[32m+[m[32mconst TabIcon = ({ tab, color, size = 21 }) => ([m
[32m+[m[32m  <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke={color} strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round">[m
[32m+[m[32m    {TAB_ICONS[tab] ? TAB_ICONS[tab](color) : null}[m
[32m+[m[32m  </svg>[m
[32m+[m[32m);[m
[32m+[m[32m// Fixed bottom tab bar — replaces the top segmented control on mobile only.[m
[32m+[m[32mfunction BottomNav({ view, setView, tabs }) {[m
[32m+[m[32m  return ([m
[32m+[m[32m    <div style={{ position: "fixed", left: 0, right: 0, bottom: 0, zIndex: 200, display: "flex", background: "rgba(248,249,251,0.94)", backdropFilter: "blur(20px) saturate(140%)", WebkitBackdropFilter: "blur(20px) saturate(140%)", borderTop: `1px solid ${T.line}`, boxShadow: "0 -2px 16px rgba(15,23,42,0.05)", paddingBottom: "env(safe-area-inset-bottom)" }}>[m
[32m+[m[32m      {tabs.map(t => {[m
[32m+[m[32m        const active = view === t;[m
[32m+[m[32m        const color = active ? T.greenDeep : T.faint;[m
[32m+[m[32m        return ([m
[32m+[m[32m          <button key={t} onClick={() => setView(t)} style={{ flex: 1, display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", gap: "3px", padding: "8px 2px 7px", background: "none", border: "none", cursor: "pointer" }}>[m
[32m+[m[32m            <TabIcon tab={t} color={color} />[m
[32m+[m[32m            <span style={{ fontSize: "9px", fontWeight: 700, color, textTransform: "uppercase", letterSpacing: "0.04em", fontFamily: syne }}>{t}</span>[m
[32m+[m[32m          </button>[m
[32m+[m[32m        );[m
[32m+[m[32m      })}[m
[32m+[m[32m    </div>[m
[32m+[m[32m  );[m
[32m+[m[32m}[m
 // ════════════════════════════════════════════════════════════════════════════[m
 // STUDIO — Shorts production. Each Short moves: idea → script → assets → ready →[m
 // posted. Claude generates the assets live; the three Short types frame the angle.[m
[36m@@ -413,7 +490,7 @@[m [masync function synthesizeInsight(recentObs, allowSonnet) {[m
   return raw;[m
 }[m
 // ─── STUDIO VIEW — the Shorts production pillar ──────────────────────────────[m
[31m-function StudioView({ shorts, setShorts }) {[m
[32m+[m[32mfunction StudioView({ shorts, setShorts, isMobile }) {[m
   const [composing, setComposing] = useState(false);[m
   const [openShort, setOpenShort] = useState(null);[m
 [m
[36m@@ -425,7 +502,7 @@[m [mfunction StudioView({ shorts, setShorts }) {[m
   const byStage = (k) => shorts.filter(s => s.stage === k);[m
 [m
   return ([m
[31m-    <div style={{ minHeight: "calc(100vh - 52px)", padding: "24px 28px" }}>[m
[32m+[m[32m    <div style={{ minHeight: "calc(100vh - 52px)", padding: viewPad(isMobile) }}>[m
       <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between", marginBottom: "20px", flexWrap: "wrap", gap: "12px" }}>[m
         <div>[m
           <div style={{ fontSize: "18px", fontWeight: 700, color: T.ink, fontFamily: syne }}>Studio</div>[m
[36m@@ -442,11 +519,11 @@[m [mfunction StudioView({ shorts, setShorts }) {[m
           <Btn primary onClick={() => setComposing(true)}>✦ Create your first Short</Btn>[m
         </Card>[m
       ) : ([m
[31m-        <div style={{ display: "grid", gridTemplateColumns: "repeat(5, 1fr)", gap: "12px", alignItems: "start" }}>[m
[32m+[m[32m        <div style={kanbanWrapStyle(isMobile, 5)}>[m
           {SHORT_STAGES.map(stage => {[m
             const items = byStage(stage.key);[m
             return ([m
[31m-              <div key={stage.key}>[m
[32m+[m[32m              <div key={stage.key} style={kanbanColStyle(isMobile)}>[m
                 <div style={{ display: "flex", alignItems: "center", gap: "7px", marginBottom: "10px", padding: "0 2px" }}>[m
                   <span style={{ width: "7px", height: "7px", borderRadius: "50%", background: stage.color }} />[m
                   <span style={{ fontSize: "11px", fontWeight: 700, color: T.ink, fontFamily: syne }}>{stage.label}</span>[m
[36m@@ -470,14 +547,14 @@[m [mfunction StudioView({ shorts, setShorts }) {[m
         </div>[m
       )}[m
 [m
[31m-      {composing && <ComposeModal onClose={() => setComposing(false)} onCreate={addShort} />}[m
[31m-      {openShort && <ShortDetail short={openShort} onClose={() => setOpenShort(null)} onUpdate={updateShort} onDelete={delShort} />}[m
[32m+[m[32m      {composing && <ComposeModal isMobile={isMobile} onClose={() => setComposing(false)} onCreate={addShort} />}[m
[32m+[m[32m      {openShort && <ShortDetail short={openShort} isMobile={isMobile} onClose={() => setOpenShort(null)} onUpdate={updateShort} onDelete={delShort} />}[m
     </div>[m
   );[m
 }[m
 [m
 // Compose: pick type + topic, generate the full package.[m
[31m-function ComposeModal({ onClose, onCreate }) {[m
[32m+[m[32mfunction ComposeModal({ onClose, onCreate, isMobile }) {[m
   const [type, setType] = useState("angle");[m
   const [topic, setTopic] = useState("");[m
   const [gen, setGen] = useState(false);[m
[36m@@ -491,13 +568,13 @@[m [mfunction ComposeModal({ onClose, onCreate }) {[m
   };[m
 [m
   return ([m
[31m-    <div onClick={onClose} style={{ position: "fixed", inset: 0, background: "rgba(11,18,32,0.5)", zIndex: 300, display: "flex", alignItems: "center", justifyContent: "center", animation: "fadein 0.15s ease both" }}>[m
[31m-      <div onClick={e => e.stopPropagation()} style={{ background: T.card, borderRadius: "18px", padding: "26px 28px", width: "560px", maxWidth: "94vw", boxShadow: "0 32px 80px rgba(11,17,32,0.24)" }}>[m
[32m+[m[32m    <ModalShell onClose={onClose} isMobile={isMobile} width={560}>[m
[32m+[m[32m      <div style={{ padding: isMobile ? "14px 18px calc(18px + env(safe-area-inset-bottom))" : "26px 28px", overflowY: "auto" }}>[m
         <div style={{ fontSize: "16px", fontWeight: 700, color: T.ink, fontFamily: syne, marginBottom: "4px" }}>New Short</div>[m
         <div style={{ fontSize: "12px", color: T.faint, marginBottom: "20px" }}>Pick a type and topic — Claude drafts the hook, script, thumbnails, title, description, and tags.</div>[m
 [m
         <Label style={{ marginBottom: "8px" }}>Short type</Label>[m
[31m-        <div style={{ display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: "8px", marginBottom: "18px" }}>[m
[32m+[m[32m        <div style={{ display: "grid", gridTemplateColumns: isMobile ? "1fr" : "repeat(3, 1fr)", gap: "8px", marginBottom: "18px" }}>[m
           {Object.entries(SHORT_TYPES).map(([k, t]) => ([m
             <button key={k} onClick={() => setType(k)} style={{ textAlign: "left", padding: "12px 13px", background: type === k ? "rgba(14,159,110,0.07)" : "#F8FAFC", border: `1px solid ${type === k ? "rgba(14,159,110,0.35)" : T.line}`, borderRadius: "11px", cursor: "pointer" }}>[m
               <div style={{ fontSize: "12px", fontWeight: 700, color: type === k ? T.greenDeep : T.ink, fontFamily: syne, marginBottom: "3px" }}>{t.label}</div>[m
[36m@@ -516,11 +593,11 @@[m [mfunction ComposeModal({ onClose, onCreate }) {[m
         </div>[m
         <div style={{ fontSize: "10px", color: T.faint, textAlign: "center", marginTop: "10px" }}>One Claude call drafts all assets. Runs on Haiku to stay cheap.</div>[m
       </div>[m
[31m-    </div>[m
[32m+[m[32m    </ModalShell>[m
   );[m
 }[m
 // Short detail — view/edit/regenerate every asset, run the publish checklist, advance stage.[m
[31m-function ShortDetail({ short, onClose, onUpdate, onDelete }) {[m
[32m+[m[32mfunction ShortDetail({ short, onClose, onUpdate, onDelete, isMobile }) {[m
   const [regen, setRegen] = useState(null); // which asset is regenerating[m
   const [tab, setTab] = useState("assets");[m
   const t = SHORT_TYPES[short.type] || SHORT_TYPES.angle;[m
[36m@@ -557,10 +634,10 @@[m [mfunction ShortDetail({ short, onClose, onUpdate, onDelete }) {[m
   );[m
   const box = { background: "#F8FAFC", border: `1px solid ${T.line}`, borderRadius: "10px", padding: "12px 14px", fontSize: "13px", color: T.ink, lineHeight: 1.6, whiteSpace: "pre-wrap" };[m
 [m
[32m+[m[32m  const hPad = isMobile ? "18px" : "24px";[m
   return ([m
[31m-    <div onClick={onClose} style={{ position: "fixed", inset: 0, background: "rgba(11,18,32,0.5)", zIndex: 300, display: "flex", alignItems: "center", justifyContent: "center", animation: "fadein 0.15s ease both" }}>[m
[31m-      <div onClick={e => e.stopPropagation()} style={{ background: T.card, borderRadius: "18px", width: "640px", maxWidth: "95vw", maxHeight: "88vh", display: "flex", flexDirection: "column", boxShadow: "0 32px 80px rgba(11,17,32,0.24)", overflow: "hidden" }}>[m
[31m-        <div style={{ padding: "18px 24px", borderBottom: `1px solid ${T.line}`, display: "flex", alignItems: "flex-start", justifyContent: "space-between", gap: "12px" }}>[m
[32m+[m[32m    <ModalShell onClose={onClose} isMobile={isMobile} width={640}>[m
[32m+[m[32m        <div style={{ padding: `18px ${hPad}`, borderBottom: `1px solid ${T.line}`, display: "flex", alignItems: "flex-start", justifyContent: "space-between", gap: "12px", flexShrink: 0 }}>[m
           <div style={{ minWidth: 0 }}>[m
             <div style={{ display: "inline-block", fontSize: "9px", fontWeight: 700, color: T.amberDeep, background: "rgba(245,158,11,0.1)", padding: "2px 7px", borderRadius: "5px", textTransform: "uppercase", letterSpacing: "0.06em", fontFamily: syne, marginBottom: "6px" }}>{t.label} · {short.stage}</div>[m
             <div style={{ fontSize: "15px", fontWeight: 700, color: T.ink, fontFamily: syne, lineHeight: 1.3 }}>{short.title || short.topic || "Untitled Short"}</div>[m
[36m@@ -568,13 +645,13 @@[m [mfunction ShortDetail({ short, onClose, onUpdate, onDelete }) {[m
           <button onClick={onClose} style={{ background: "none", border: "none", color: "#CBD5E1", fontSize: "20px", cursor: "pointer", lineHeight: 1, flexShrink: 0 }}>×</button>[m
         </div>[m
 [m
[31m-        <div style={{ display: "flex", gap: "4px", padding: "12px 24px 0" }}>[m
[32m+[m[32m        <div style={{ display: "flex", gap: "4px", padding: `12px ${hPad} 0`, flexShrink: 0 }}>[m
           {[["assets", "Assets"], ["publish", `Publish (${checkDone}/${PUBLISH_CHECKLIST.length})`]].map(([k, l]) => ([m
             <button key={k} onClick={() => setTab(k)} style={{ padding: "7px 14px", background: tab === k ? "rgba(14,159,110,0.08)" : "transparent", border: "none", borderRadius: "8px", color: tab === k ? T.greenDeep : T.faint, fontSize: "12px", fontWeight: 700, cursor: "pointer", fontFamily: syne }}>{l}</button>[m
           ))}[m
         </div>[m
 [m
[31m-        <div style={{ padding: "18px 24px", overflowY: "auto" }}>[m
[32m+[m[32m        <div style={{ padding: `18px ${hPad}`, overflowY: "auto" }}>[m
           {tab === "assets" ? ([m
             <>[m
               {!short.script && short.stage === "idea" ? ([m
[36m@@ -621,15 +698,14 @@[m [mfunction ShortDetail({ short, onClose, onUpdate, onDelete }) {[m
           )}[m
         </div>[m
 [m
[31m-        <div style={{ padding: "14px 24px", borderTop: `1px solid ${T.line}`, display: "flex", alignItems: "center", justifyContent: "space-between", gap: "10px" }}>[m
[32m+[m[32m        <div style={{ padding: `14px ${hPad}`, borderTop: `1px solid ${T.line}`, display: "flex", alignItems: "center", justifyContent: "space-between", gap: "10px", flexWrap: "wrap", flexShrink: 0 }}>[m
           <button onClick={() => onDelete(short.id)} style={{ background: "none", border: "none", color: "#CBD5E1", fontSize: "11px", cursor: "pointer", fontWeight: 600 }}>Delete</button>[m
           <div style={{ display: "flex", gap: "8px" }}>[m
             <Btn onClick={copyAll}>Copy all</Btn>[m
[31m-            {short.stage !== "posted" && <Btn primary onClick={advance}>{short.stage === "ready" ? "Mark posted →" : "Advance stage →"}</Btn>}[m
[32m+[m[32m            {short.stage !== "posted" && <Btn primary onClick={advance}>{short.stage === "ready" ? "Mark posted →" : isMobile ? "Advance →" : "Advance stage →"}</Btn>}[m
           </div>[m
         </div>[m
[31m-      </div>[m
[31m-    </div>[m
[32m+[m[32m    </ModalShell>[m
   );[m
 }[m
 // ─── CREATORS VIEW — outreach pipeline (YouTube creators) ────────────────────[m
[36m@@ -640,7 +716,7 @@[m [mconst CREATOR_STAGES = [[m
   { key: "replied", label: "Replied", color: "#EC4899" },[m
   { key: "collab", label: "Collab", color: "#0E9F6E" },[m
 ];[m
[31m-function CreatorsView({ creators, setCreators }) {[m
[32m+[m[32mfunction CreatorsView({ creators, setCreators, isMobile }) {[m
   const [adding, setAdding] = useState(false);[m
   const [sortBy, setSortBy] = useState("value");[m
   const save = (next) => { setCreators(next); sm.set("creators", next); };[m
[36m@@ -655,7 +731,7 @@[m [mfunction CreatorsView({ creators, setCreators }) {[m
   const totalReach = creators.filter(c => !["rejected"].includes(c.stage)).reduce((s, c) => s + creatorValue(c).score, 0);[m
 [m
   return ([m
[31m-    <div style={{ minHeight: "calc(100vh - 52px)", padding: "24px 28px" }}>[m
[32m+[m[32m    <div style={{ minHeight: "calc(100vh - 52px)", padding: viewPad(isMobile) }}>[m
       <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between", marginBottom: "16px", flexWrap: "wrap", gap: "12px" }}>[m
         <div>[m
           <div style={{ fontSize: "18px", fontWeight: 700, color: T.ink, fontFamily: syne }}>Creators</div>[m
[36m@@ -685,11 +761,11 @@[m [mfunction CreatorsView({ creators, setCreators }) {[m
           <Btn primary onClick={() => setAdding(true)}>+ Add your first creator</Btn>[m
         </Card>[m
       ) : ([m
[31m-        <div style={{ display: "grid", gridTemplateColumns: "repeat(5, 1fr)", gap: "12px", alignItems: "start" }}>[m
[32m+[m[32m        <div style={kanbanWrapStyle(isMobile, 5)}>[m
           {CREATOR_STAGES.map(stage => {[m
             const items = byStage(stage.key);[m
             return ([m
[31m-              <div key={stage.key}>[m
[32m+[m[32m              <div key={stage.key} style={kanbanColStyle(isMobile)}>[m
                 <div style={{ display: "flex", alignItems: "center", gap: "7px", marginBottom: "10px", padding: "0 2px" }}>[m
                   <span style={{ width: "7px", height: "7px", borderRadius: "50%", background: stage.color }} />[m
                   <span style={{ fontSize: "11px", fontWeight: 700, color: T.ink, fontFamily: syne }}>{stage.label}</span>[m
[36m@@ -718,20 +794,20 @@[m [mfunction CreatorsView({ creators, setCreators }) {[m
         </div>[m
       )}[m
 [m
[31m-      {adding && <AddCreatorModal onClose={() => setAdding(false)} onAdd={(c) => { save([{ id: `c_${Date.now()}`, stage: "prospected", status: "prospected", created_at: new Date().toISOString(), ...c }, ...creators]); setAdding(false); }} />}[m
[32m+[m[32m      {adding && <AddCreatorModal isMobile={isMobile} onClose={() => setAdding(false)} onAdd={(c) => { save([{ id: `c_${Date.now()}`, stage: "prospected", status: "prospected", created_at: new Date().toISOString(), ...c }, ...creators]); setAdding(false); }} />}[m
     </div>[m
   );[m
 }[m
 [m
[31m-function AddCreatorModal({ onClose, onAdd }) {[m
[32m+[m[32mfunction AddCreatorModal({ onClose, onAdd, isMobile }) {[m
   const [f, setF] = useState({ channel_name: "", subscriber_count: "", niche: "", description: "", engagement_rate: "" });[m
   const set = (k, v) => setF(p => ({ ...p, [k]: v }));[m
   const submit = () => { if (!f.channel_name) return; onAdd({ ...f, subscriber_count: Number(f.subscriber_count) || 0, engagement_rate: f.engagement_rate ? Number(f.engagement_rate) / 100 : null }); };[m
   const v = f.channel_name ? creatorValue({ ...f, subscriber_count: Number(f.subscriber_count) || 0 }) : null;[m
   const input = { width: "100%", padding: "10px 12px", border: `1px solid ${T.line}`, borderRadius: "9px", fontSize: "13px", color: T.ink, marginBottom: "10px" };[m
   return ([m
[31m-    <div onClick={onClose} style={{ position: "fixed", inset: 0, background: "rgba(11,18,32,0.5)", zIndex: 300, display: "flex", alignItems: "center", justifyContent: "center", animation: "fadein 0.15s ease both" }}>[m
[31m-      <div onClick={e => e.stopPropagation()} style={{ background: T.card, borderRadius: "18px", padding: "26px 28px", width: "480px", maxWidth: "94vw", boxShadow: "0 32px 80px rgba(11,17,32,0.24)" }}>[m
[32m+[m[32m    <ModalShell onClose={onClose} isMobile={isMobile} width={480}>[m
[32m+[m[32m      <div style={{ padding: isMobile ? "14px 18px calc(18px + env(safe-area-inset-bottom))" : "26px 28px", overflowY: "auto" }}>[m
         <div style={{ fontSize: "16px", fontWeight: 700, color: T.ink, fontFamily: syne, marginBottom: "18px" }}>Add Creator</div>[m
         <input placeholder="Channel name" value={f.channel_name} onChange={e => set("channel_name", e.target.value)} style={input} />[m
         <input placeholder="Subscriber count (e.g. 45000)" value={f.subscriber_count} onChange={e => set("subscriber_count", e.target.value)} style={input} />[m
[36m@@ -744,7 +820,7 @@[m [mfunction AddCreatorModal({ onClose, onAdd }) {[m
           <Btn onClick={onClose} style={{ padding: "11px 18px" }}>Cancel</Btn>[m
         </div>[m
       </div>[m
[31m-    </div>[m
[32m+[m[32m    </ModalShell>[m
   );[m
 }[m
 // ─── AGENT ENGINE (headless) ─────────────────────────────────────────────────[m
[36m@@ -811,7 +887,7 @@[m [mfunction AgentEngine({ creators, shorts, articles, onArticleDraft }) {[m
 }[m
 [m
 // ─── AGENTS VIEW (control panel + feed) ──────────────────────────────────────[m
[31m-function AgentsView() {[m
[32m+[m[32mfunction AgentsView({ isMobile }) {[m
   const [ctrl, setCtrl] = useState(() => eng.get());[m
   const [feed, setFeed] = useState(() => kb.all());[m
   const [passFlash, setPassFlash] = useState(false);[m
[36m@@ -830,12 +906,12 @@[m [mfunction AgentsView() {[m
   const agentMeta = [["creatorScout","Creator Scout","Prime-fit creators un-contacted"],["production","Production Watcher","Shorts stuck or unscheduled"],["cadence","Cadence Monitor","Posting gaps"],["reply","Reply Sentinel","Creator replies waiting"],["pattern","Pattern Learner","Which Short types you produce"],["cost","Cost Sentinel","AI spend guardrail"]];[m
 [m
   return ([m
[31m-    <div style={{ minHeight: "calc(100vh - 52px)", padding: "24px 28px" }}>[m
[32m+[m[32m    <div style={{ minHeight: "calc(100vh - 52px)", padding: viewPad(isMobile) }}>[m
       <div style={{ marginBottom: "20px" }}>[m
         <div style={{ fontSize: "18px", fontWeight: 700, color: T.ink, fontFamily: syne }}>Agent Engine</div>[m
         <div style={{ fontSize: "12px", color: T.faint, marginTop: "2px" }}>A living roster watching creators + studio on a free heartbeat, spending tokens only when it's worth it.</div>[m
       </div>[m
[31m-      <div style={{ background: ctrl.running ? T.navyGrad : T.card, border: ctrl.running ? "none" : `1px solid ${T.line}`, borderRadius: "16px", padding: "18px 22px", marginBottom: "16px", display: "flex", alignItems: "center", justifyContent: "space-between", gap: "16px", flexWrap: "wrap", boxShadow: T.cardShadow }}>[m
[32m+[m[32m      <div style={{ background: ctrl.running ? T.navyGrad : T.card, border: ctrl.running ? "none" : `1px solid ${T.line}`, borderRadius: "16px", padding: isMobile ? "16px 18px" : "18px 22px", marginBottom: "16px", display: "flex", alignItems: "center", justifyContent: "space-between", gap: "16px", flexWrap: "wrap", boxShadow: T.cardShadow }}>[m
         <div style={{ display: "flex", alignItems: "center", gap: "16px" }}>[m
           <button onClick={() => update({ running: !ctrl.running })} style={{ width: "52px", height: "52px", borderRadius: "50%", border: "none", background: ctrl.running ? T.green : T.navy, color: "#FFF", fontSize: "20px", cursor: "pointer", flexShrink: 0 }}>{ctrl.running ? "⏸" : "▶"}</button>[m
           <div>[m
[36m@@ -848,7 +924,7 @@[m [mfunction AgentsView() {[m
           <button onClick={runOnce} style={{ padding: "9px 14px", background: ctrl.running ? "rgba(255,255,255,0.1)" : "rgba(15,23,42,0.04)", border: ctrl.running ? "1px solid rgba(255,255,255,0.2)" : `1px solid ${T.line}`, borderRadius: "9px", color: ctrl.running ? "#E8EDF7" : T.sub, fontSize: "11px", fontWeight: 700, cursor: "pointer", fontFamily: syne }}>⚡ Run pass now</button>[m
         </div>[m
       </div>[m
[31m-      <div style={{ display: "grid", gridTemplateColumns: "300px 1fr", gap: "16px", alignItems: "start" }}>[m
[32m+[m[32m      <div style={{ display: "grid", gridTemplateColumns: isMobile ? "1fr" : "300px 1fr", gap: "16px", alignItems: "start" }}>[m
         <div style={{ display: "flex", flexDirection: "column", gap: "10px" }}>[m
           <Label style={{ marginBottom: "2px" }}>Token Controls</Label>[m
           <Toggle on={ctrl.observeOnly} onClick={() => update({ observeOnly: !ctrl.observeOnly })} label="Observe-only" sub="Heuristics only — never spend tokens" />[m
[36m@@ -881,7 +957,7 @@[m [mfunction AgentsView() {[m
   );[m
 }[m
 // ─── SEO VIEW — article pipeline with approval gate ──────────────────────────[m
[31m-function SeoView({ articles, setArticles }) {[m
[32m+[m[32mfunction SeoView({ articles, setArticles, isMobile }) {[m
   const [composing, setComposing] = useState(false);[m
   const [openArticle, setOpenArticle] = useState(null);[m
   const [keywords, setKeywords] = useState(() => sm.get("seo_keywords") || "");[m
[36m@@ -895,7 +971,7 @@[m [mfunction SeoView({ articles, setArticles }) {[m
   const nextAuto = autoDraft && lastAuto ? new Date(lastAuto + everyDays * 86400000) : null;[m
 [m
   return ([m
[31m-    <div style={{ minHeight: "calc(100vh - 52px)", padding: "24px 28px" }}>[m
[32m+[m[32m    <div style={{ minHeight: "calc(100vh - 52px)", padding: viewPad(isMobile) }}>[m
       <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between", marginBottom: "16px", flexWrap: "wrap", gap: "12px" }}>[m
         <div>[m
           <div style={{ fontSize: "18px", fontWeight: 700, color: T.ink, fontFamily: syne }}>SEO</div>[m
[36m@@ -905,7 +981,7 @@[m [mfunction SeoView({ articles, setArticles }) {[m
       </div>[m
 [m
       {/* Auto-draft cadence + keyword panel */}[m
[31m-      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "14px", marginBottom: "18px" }}>[m
[32m+[m[32m      <div style={{ display: "grid", gridTemplateColumns: isMobile ? "1fr" : "1fr 1fr", gap: isMobile ? "10px" : "14px", marginBottom: "18px" }}>[m
         <Card>[m
           <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: "10px" }}>[m
             <Label>Auto-draft cadence</Label>[m
[36m@@ -938,11 +1014,11 @@[m [mfunction SeoView({ articles, setArticles }) {[m
           <Btn primary onClick={() => setComposing(true)}>✦ Draft the first article</Btn>[m
         </Card>[m
       ) : ([m
[31m-        <div style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: "12px", alignItems: "start" }}>[m
[32m+[m[32m        <div style={kanbanWrapStyle(isMobile, 4)}>[m
           {ARTICLE_STAGES.map(stage => {[m
             const items = byStage(stage.key);[m
             return ([m
[31m-              <div key={stage.key}>[m
[32m+[m[32m              <div key={stage.key} style={kanbanColStyle(isMobile)}>[m
                 <div style={{ display: "flex", alignItems: "center", gap: "7px", marginBottom: "10px", padding: "0 2px" }}>[m
                   <span style={{ width: "7px", height: "7px", borderRadius: "50%", background: stage.color }} />[m
                   <span style={{ fontSize: "11px", fontWeight: 700, color: T.ink, fontFamily: syne }}>{stage.label}</span>[m
[36m@@ -963,13 +1039,13 @@[m [mfunction SeoView({ articles, setArticles }) {[m
         </div>[m
       )}[m
 [m
[31m-      {composing && <ComposeArticleModal keywords={keywords} onClose={() => setComposing(false)} onCreate={(a) => { save([{ id: `a_${Date.now()}`, created_at: new Date().toISOString(), ...a }, ...articles]); setComposing(false); }} />}[m
[31m-      {openArticle && <ArticleDetail article={openArticle} onClose={() => setOpenArticle(null)} onUpdate={update} onDelete={(id) => { save(articles.filter(a => a.id !== id)); setOpenArticle(null); }} />}[m
[32m+[m[32m      {composing && <ComposeArticleModal keywords={keywords} isMobile={isMobile} onClose={() => setComposing(false)} onCreate={(a) => { save([{ id: `a_${Date.now()}`, created_at: new Date().toISOString(), ...a }, ...articles]); setComposing(false); }} />}[m
[32m+[m[32m      {openArticle && <ArticleDetail article={openArticle} isMobile={isMobile} onClose={() => setOpenArticle(null)} onUpdate={update} onDelete={(id) => { save(articles.filter(a => a.id !== id)); setOpenArticle(null); }} />}[m
     </div>[m
   );[m
 }[m
 [m
[31m-function ComposeArticleModal({ keywords, onClose, onCreate }) {[m
[32m+[m[32mfunction ComposeArticleModal({ keywords, onClose, onCreate, isMobile }) {[m
   const kwList = (keywords || "").split("\n").map(k => k.trim()).filter(Boolean);[m
   const [keyword, setKeyword] = useState(kwList[0] || "");[m
   const [notes, setNotes] = useState("");[m
[36m@@ -982,8 +1058,8 @@[m [mfunction ComposeArticleModal({ keywords, onClose, onCreate }) {[m
     else onCreate({ keyword, stage: "idea" });[m
   };[m
   return ([m
[31m-    <div onClick={onClose} style={{ position: "fixed", inset: 0, background: "rgba(11,18,32,0.5)", zIndex: 300, display: "flex", alignItems: "center", justifyContent: "center", animation: "fadein 0.15s ease both" }}>[m
[31m-      <div onClick={e => e.stopPropagation()} style={{ background: T.card, borderRadius: "18px", padding: "26px 28px", width: "520px", maxWidth: "94vw", boxShadow: "0 32px 80px rgba(11,17,32,0.24)" }}>[m
[32m+[m[32m    <ModalShell onClose={onClose} isMobile={isMobile} width={520}>[m
[32m+[m[32m      <div style={{ padding: isMobile ? "14px 18px calc(18px + env(safe-area-inset-bottom))" : "26px 28px", overflowY: "auto" }}>[m
         <div style={{ fontSize: "16px", fontWeight: 700, color: T.ink, fontFamily: syne, marginBottom: "4px" }}>New Article</div>[m
         <div style={{ fontSize: "12px", color: T.faint, marginBottom: "18px" }}>Claude drafts the full package — title tag, meta, outline, article, internal links — into your review queue.</div>[m
         <Label style={{ marginBottom: "8px" }}>Target keyword</Label>[m
[36m@@ -1002,11 +1078,11 @@[m [mfunction ComposeArticleModal({ keywords, onClose, onCreate }) {[m
           <Btn onClick={onClose} style={{ padding: "12px 18px" }}>Cancel</Btn>[m
         </div>[m
       </div>[m
[31m-    </div>[m
[32m+[m[32m    </ModalShell>[m
   );[m
 }[m
 [m
[31m-function ArticleDetail({ article, onClose, onUpdate, onDelete }) {[m
[32m+[m[32mfunction ArticleDetail({ article, onClose, onUpdate, onDelete, isMobile }) {[m
   const [publishing, setPublishing] = useState(false);[m
   const [pubResult, setPubResult] = useState(null);[m
   const stage = article.stage || "idea";[m
[36m@@ -1022,10 +1098,10 @@[m [mfunction ArticleDetail({ article, onClose, onUpdate, onDelete }) {[m
     setPublishing(false);[m
   };[m
   const box = { background: "#F8FAFC", border: `1px solid ${T.line}`, borderRadius: "10px", padding: "12px 14px", fontSize: "13px", color: T.ink, lineHeight: 1.6 };[m
[32m+[m[32m  const hPad = isMobile ? "18px" : "24px";[m
   return ([m
[31m-    <div onClick={onClose} style={{ position: "fixed", inset: 0, background: "rgba(11,18,32,0.5)", zIndex: 300, display: "flex", alignItems: "center", justifyContent: "center", animation: "fadein 0.15s ease both" }}>[m
[31m-      <div onClick={e => e.stopPropagation()} style={{ background: T.card, borderRadius: "18px", width: "720px", maxWidth: "95vw", maxHeight: "88vh", display: "flex", flexDirection: "column", boxShadow: "0 32px 80px rgba(11,17,32,0.24)", overflow: "hidden" }}>[m
[31m-        <div style={{ padding: "18px 24px", borderBottom: `1px solid ${T.line}`, display: "flex", justifyContent: "space-between", alignItems: "flex-start", gap: "12px" }}>[m
[32m+[m[32m    <ModalShell onClose={onClose} isMobile={isMobile} width={720}>[m
[32m+[m[32m        <div style={{ padding: `18px ${hPad}`, borderBottom: `1px solid ${T.line}`, display: "flex", justifyContent: "space-between", alignItems: "flex-start", gap: "12px", flexShrink: 0 }}>[m
           <div style={{ minWidth: 0 }}>[m
             <div style={{ display: "inline-block", fontSize: "9px", fontWeight: 700, color: T.amberDeep, background: "rgba(245,158,11,0.1)", padding: "2px 7px", borderRadius: "5px", textTransform: "uppercase", letterSpacing: "0.06em", fontFamily: syne, marginBottom: "6px" }}>{stage}{article.auto_drafted ? " · agent draft" : ""}</div>[m
             <div style={{ fontSize: "15px", fontWeight: 700, color: T.ink, fontFamily: syne, lineHeight: 1.3 }}>{article.title_tag || article.keyword || "Untitled"}</div>[m
[36m@@ -1033,8 +1109,8 @@[m [mfunction ArticleDetail({ article, onClose, onUpdate, onDelete }) {[m
           </div>[m
           <button onClick={onClose} style={{ background: "none", border: "none", color: "#CBD5E1", fontSize: "20px", cursor: "pointer", lineHeight: 1, flexShrink: 0 }}>×</button>[m
         </div>[m
[31m-        <div style={{ padding: "18px 24px", overflowY: "auto" }}>[m
[31m-          <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "10px", marginBottom: "16px" }}>[m
[32m+[m[32m        <div style={{ padding: `18px ${hPad}`, overflowY: "auto" }}>[m
[32m+[m[32m          <div style={{ display: "grid", gridTemplateColumns: isMobile ? "1fr" : "1fr 1fr", gap: "10px", marginBottom: "16px" }}>[m
             <div><Label style={{ marginBottom: "6px" }}>Title tag</Label><div style={box}>{article.title_tag || "—"}</div></div>[m
             <div><Label style={{ marginBottom: "6px" }}>Slug</Label><div style={{ ...box, fontFamily: mono, fontSize: "12px" }}>/{article.slug || "—"}</div></div>[m
           </div>[m
[36m@@ -1045,10 +1121,10 @@[m [mfunction ArticleDetail({ article, onClose, onUpdate, onDelete }) {[m
             <div style={{ ...box, marginBottom: "16px" }}>{article.internal_links.map((l, i) => <div key={i} style={{ fontSize: "12px" }}>"{l.anchor}" → <span style={{ fontFamily: mono, color: T.greenDeep }}>{l.target}</span></div>)}</div>[m
           </>)}[m
           <Label style={{ marginBottom: "6px" }}>Article</Label>[m
[31m-          <div style={{ ...box, maxHeight: "320px", overflowY: "auto" }} dangerouslySetInnerHTML={{ __html: article.article_html || "<em>No draft yet.</em>" }} />[m
[32m+[m[32m          <div style={{ ...box, maxHeight: isMobile ? "44vh" : "320px", overflowY: "auto" }} dangerouslySetInnerHTML={{ __html: article.article_html || "<em>No draft yet.</em>" }} />[m
           {pubResult && <div style={{ marginTop: "12px", padding: "10px 14px", borderRadius: "9px", fontSize: "12px", background: pubResult.error ? "rgba(220,38,38,0.07)" : "rgba(14,159,110,0.07)", color: pubResult.error ? T.red : T.greenDeep, border: `1px solid ${pubResult.error ? "rgba(220,38,38,0.2)" : "rgba(14,159,110,0.25)"}` }}>{pubResult.error ? `Publish failed: ${pubResult.error}` : pubResult.method === "clipboard" ? "Local mode — article HTML copied to clipboard. Paste into Shopify admin → Blog posts → Add." : "Published to Shopify ✓"}</div>}[m
         </div>[m
[31m-        <div style={{ padding: "14px 24px", borderTop: `1px solid ${T.line}`, display: "flex", justifyContent: "space-between", alignItems: "center", gap: "10px" }}>[m
[32m+[m[32m        <div style={{ padding: `14px ${hPad}`, borderTop: `1px solid ${T.line}`, display: "flex", justifyContent: "space-between", alignItems: "center", gap: "10px", flexWrap: "wrap", flexShrink: 0 }}>[m
           <button onClick={() => onDelete(article.id)} style={{ background: "none", border: "none", color: "#CBD5E1", fontSize: "11px", cursor: "pointer", fontWeight: 600 }}>Delete</button>[m
           <div style={{ display: "flex", gap: "8px" }}>[m
             {stage === "review" && <><Btn onClick={reject}>✕ Reject</Btn><Btn primary onClick={approve}>✓ Approve</Btn></>}[m
[36m@@ -1056,13 +1132,12 @@[m [mfunction ArticleDetail({ article, onClose, onUpdate, onDelete }) {[m
             {stage === "published" && article.published_url && <a href={article.published_url} target="_blank" rel="noopener" style={{ fontSize: "12px", color: T.greenDeep, fontWeight: 700, textDecoration: "none", padding: "10px 16px" }}>View live ›</a>}[m
           </div>[m
         </div>[m
[31m-      </div>[m
[31m-    </div>[m
[32m+[m[32m    </ModalShell>[m
   );[m
 }[m
 [m
 // ─── MISSION — command center across both pillars ────────────────────────────[m
[31m-function MissionView({ creators, shorts, onNavigate }) {[m
[32m+[m[32mfunction MissionView({ creators, shorts, onNavigate, isMobile }) {[m
   const engCtrl = eng.get();[m
   const kbAll = kb.all();[m
   const cPipe = { prospected: creators.filter(c => (c.stage||"prospected") === "prospected").length, sent: creators.filter(c => c.stage === "sent").length, replied: creators.filter(c => c.stage === "replied").length, collab: creators.filter(c => c.stage === "collab").length };[m
[36m@@ -1076,19 +1151,20 @@[m [mfunction MissionView({ creators, shorts, onNavigate }) {[m
   const Stat = ({ label, value, sub, accent }) => ([m
     <Card><Label style={{ marginBottom: "10px" }}>{label}</Label><div style={{ fontSize: "26px", fontWeight: 500, color: accent || T.ink, fontFamily: mono, lineHeight: 1 }}>{value}</div>{sub && <div style={{ fontSize: "10px", color: T.faint, marginTop: "6px" }}>{sub}</div>}</Card>[m
   );[m
[32m+[m[32m  const spanMobile = isMobile ? { gridColumn: "1 / -1" } : undefined;[m
 [m
   return ([m
[31m-    <div style={{ minHeight: "calc(100vh - 52px)", padding: "24px 28px" }}>[m
[31m-      <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between", marginBottom: "18px" }}>[m
[32m+[m[32m    <div style={{ minHeight: "calc(100vh - 52px)", padding: viewPad(isMobile) }}>[m
[32m+[m[32m      <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between", marginBottom: "18px", flexWrap: "wrap", gap: "8px" }}>[m
         <div>[m
[31m-          <div style={{ fontSize: "24px", fontWeight: 700, color: T.ink, fontFamily: syne }}>{greeting}, Cameron</div>[m
[32m+[m[32m          <div style={{ fontSize: isMobile ? "20px" : "24px", fontWeight: 700, color: T.ink, fontFamily: syne }}>{greeting}, Cameron</div>[m
           <div style={{ fontSize: "12px", color: T.faint, marginTop: "2px" }}>{new Date().toLocaleDateString("en-US", { weekday: "long", month: "long", day: "numeric" })} · Zero To Secure</div>[m
         </div>[m
         <div style={{ display: "flex", alignItems: "center", gap: "6px", fontSize: "11px", color: T.green, fontWeight: 600 }}><span style={{ width: "7px", height: "7px", borderRadius: "50%", background: T.green }} />Live</div>[m
       </div>[m
 [m
[31m-      <div style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: "14px", marginBottom: "16px" }}>[m
[31m-        <Card>[m
[32m+[m[32m      <div style={{ display: "grid", gridTemplateColumns: isMobile ? "1fr 1fr" : "repeat(4, 1fr)", gap: isMobile ? "10px" : "14px", marginBottom: "16px" }}>[m
[32m+[m[32m        <Card style={spanMobile}>[m
           <Label style={{ marginBottom: "12px" }}>Creator Pipeline</Label>[m
           <div style={{ display: "flex", justifyContent: "space-between" }}>[m
             {[["Prospected", cPipe.prospected, T.faint], ["Sent", cPipe.sent, T.blue], ["Replied", cPipe.replied, "#EC4899"], ["Collab", cPipe.collab, T.green]].map(([l, v, c], i) => ([m
[36m@@ -1097,7 +1173,7 @@[m [mfunction MissionView({ creators, shorts, onNavigate }) {[m
           </div>[m
           <div style={{ fontSize: "10px", color: T.faint, marginTop: "12px", textAlign: "center" }}><span style={{ color: T.green, fontWeight: 600 }}>{fmtSubs(totalReach)} weighted reach</span> in pipeline</div>[m
         </Card>[m
[31m-        <Card>[m
[32m+[m[32m        <Card style={spanMobile}>[m
           <Label style={{ marginBottom: "12px" }}>Shorts Studio</Label>[m
           <div style={{ display: "flex", justifyContent: "space-between" }}>[m
             {[["In Production", sPipe.wip, T.amber], ["Ready", sPipe.ready, T.green], ["Posted", sPipe.posted, T.purple]].map(([l, v, c], i) => ([m
[36m@@ -1110,7 +1186,7 @@[m [mfunction MissionView({ creators, shorts, onNavigate }) {[m
         <Stat label="AI Spend" value={`$${obs.getAll().reduce((s,l) => s + (l.costEstimate||0), 0).toFixed(2)}`} sub={`${obs.getAll().length} calls`} />[m
       </div>[m
 [m
[31m-      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "16px" }}>[m
[32m+[m[32m      <div style={{ display: "grid", gridTemplateColumns: isMobile ? "1fr" : "1fr 1fr", gap: isMobile ? "12px" : "16px" }}>[m
         <Card>[m
           <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: "14px" }}><Label>Agent Roster</Label><span onClick={() => onNavigate("agents")} style={{ fontSize: "10px", color: T.amberDeep, cursor: "pointer", fontWeight: 700 }}>Engine ›</span></div>[m
           <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "8px" }}>[m
[36m@@ -1142,18 +1218,18 @@[m [mfunction MissionView({ creators, shorts, onNavigate }) {[m
 }[m
 [m
 // ─── OPS — observability ─────────────────────────────────────────────────────[m
[31m-function OpsView() {[m
[32m+[m[32mfunction OpsView({ isMobile }) {[m
   const [logs, setLogs] = useState(obs.getAll());[m
   const testLog = () => { const models = ["claude-haiku-4-5-20251001","claude-sonnet-4-6"]; const m = models[Math.floor(Math.random()*2)]; const i = 800 + Math.floor(Math.random()*3000), o = 200 + Math.floor(Math.random()*1200); obs.log({ fn: ["generate_short","regen_asset","agent_synthesis"][Math.floor(Math.random()*3)], model: m, inputTokens: i, outputTokens: o, costEstimate: estimateCost(m,i,o), latencyMs: 600+Math.floor(Math.random()*5000), ok: Math.random()>0.08 }); setLogs(obs.getAll()); };[m
   const total = logs.reduce((s,l) => s + (l.costEstimate||0), 0);[m
   const ok = logs.filter(l => l.ok !== false).length;[m
   return ([m
[31m-    <div style={{ minHeight: "calc(100vh - 52px)", padding: "24px 28px" }}>[m
[31m-      <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start", marginBottom: "20px" }}>[m
[32m+[m[32m    <div style={{ minHeight: "calc(100vh - 52px)", padding: viewPad(isMobile) }}>[m
[32m+[m[32m      <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start", marginBottom: "20px", flexWrap: "wrap", gap: "10px" }}>[m
         <div><div style={{ fontSize: "18px", fontWeight: 700, color: T.ink, fontFamily: syne }}>Observability</div><div style={{ fontSize: "12px", color: T.faint, marginTop: "2px" }}>Every Claude call — tokens, cost, latency, success.</div></div>[m
         <div style={{ display: "flex", gap: "8px" }}><Btn primary onClick={testLog}>+ Test log</Btn><Btn onClick={() => setLogs(obs.getAll())}>↻ Refresh</Btn><Btn onClick={() => { obs.clear(); setLogs([]); }}>Clear</Btn></div>[m
       </div>[m
[31m-      <div style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: "14px", marginBottom: "16px" }}>[m
[32m+[m[32m      <div style={{ display: "grid", gridTemplateColumns: isMobile ? "1fr 1fr" : "repeat(4, 1fr)", gap: isMobile ? "10px" : "14px", marginBottom: "16px" }}>[m
         {[["Total Calls", logs.length], ["Est. Cost", `$${total.toFixed(3)}`], ["Success", logs.length ? `${Math.round(ok/logs.length*100)}%` : "—"], ["Avg Latency", logs.length ? `${Math.round(logs.reduce((s,l)=>s+(l.latencyMs||0),0)/logs.length)}ms` : "—"]].map(([l, v], i) => ([m
           <Card key={i}><Label style={{ marginBottom: "10px" }}>{l}</Label><div style={{ fontSize: "24px", fontWeight: 500, color: T.ink, fontFamily: mono }}>{v}</div></Card>[m
         ))}[m
[36m@@ -1162,7 +1238,16 @@[m [mfunction OpsView() {[m
         <Label style={{ marginBottom: "12px" }}>Run Log</Label>[m
         {logs.length === 0 ? <div style={{ fontSize: "13px", color: T.faint, textAlign: "center", padding: "24px 0" }}>No calls logged. Hit "Test log" to verify tracking works.</div> : ([m
           <div style={{ display: "flex", flexDirection: "column", gap: "2px" }}>[m
[31m-            {logs.slice(0, 30).map((l, i) => ([m
[32m+[m[32m            {logs.slice(0, 30).map((l, i) => isMobile ? ([m
[32m+[m[32m              <div key={i} style={{ padding: "9px 2px", borderBottom: i < 29 ? `1px solid ${T.line}` : "none" }}>[m
[32m+[m[32m                <div style={{ display: "flex", alignItems: "center", gap: "9px" }}>[m
[32m+[m[32m                  <span style={{ width: "6px", height: "6px", borderRadius: "50%", background: l.ok === false ? T.red : T.green, flexShrink: 0 }} />[m
[32m+[m[32m                  <span style={{ fontSize: "12px", color: T.ink, fontWeight: 600, flex: 1, minWidth: 0, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{l.fn}</span>[m
[32m+[m[32m                  <span style={{ fontSize: "11px", color: T.ink, fontFamily: mono, flexShrink: 0 }}>${(l.costEstimate||0).toFixed(4)}</span>[m
[32m+[m[32m                </div>[m
[32m+[m[32m                <div style={{ fontSize: "10px", color: T.faint, fontFamily: mono, marginTop: "3px", paddingLeft: "15px" }}>{l.model?.includes("sonnet") ? "Sonnet" : "Haiku"} · {l.latencyMs}ms</div>[m
[32m+[m[32m              </div>[m
[32m+[m[32m            ) : ([m
               <div key={i} style={{ display: "flex", alignItems: "center", gap: "12px", padding: "9px 4px", borderBottom: i < 29 ? `1px solid ${T.line}` : "none" }}>[m
                 <span style={{ width: "6px", height: "6px", borderRadius: "50%", background: l.ok === false ? T.red : T.green, flexShrink: 0 }} />[m
                 <span style={{ fontSize: "12px", color: T.ink, fontWeight: 600, flex: 1 }}>{l.fn}</span>[m
[36m@@ -1181,6 +1266,7 @@[m [mfunction OpsView() {[m
 // ─── APP SHELL ───────────────────────────────────────────────────────────────[m
 export default function App() {[m
   useGlobalStyles();[m
[32m+[m[32m  const isMobile = useIsMobile();[m
   const [view, setView] = useState("mission");[m
   const [creators, setCreators] = useState(() => sm.get("creators") || []);[m
   const [shorts, setShorts] = useState(() => sm.get("shorts") || []);[m
[36m@@ -1190,27 +1276,30 @@[m [mexport default function App() {[m
   const TABS = ["mission", "creators", "studio", "seo", "agents", "ops"];[m
 [m
   return ([m
[31m-    <div style={{ minHeight: "100vh", fontFamily: "'Inter', system-ui, sans-serif" }}>[m
[32m+[m[32m    <div style={{ minHeight: "100vh", fontFamily: "'Inter', system-ui, sans-serif", paddingBottom: isMobile ? "calc(60px + env(safe-area-inset-bottom))" : 0 }}>[m
       <AgentEngine creators={creators} shorts={shorts} articles={articles} onArticleDraft={addArticle} />[m
[31m-      <div style={{ borderBottom: `1px solid ${T.line}`, padding: "0 24px", height: "52px", display: "flex", alignItems: "center", justifyContent: "space-between", position: "sticky", top: 0, background: "rgba(248,249,251,0.82)", backdropFilter: "blur(20px) saturate(140%)", WebkitBackdropFilter: "blur(20px) saturate(140%)", boxShadow: "0 1px 0 rgba(15,23,42,0.02), 0 4px 16px rgba(15,23,42,0.03)", zIndex: 50 }}>[m
[32m+[m[32m      <div style={{ borderBottom: `1px solid ${T.line}`, padding: isMobile ? "0 16px" : "0 24px", height: "52px", display: "flex", alignItems: "center", justifyContent: "space-between", position: "sticky", top: 0, background: "rgba(248,249,251,0.82)", backdropFilter: "blur(20px) saturate(140%)", WebkitBackdropFilter: "blur(20px) saturate(140%)", boxShadow: "0 1px 0 rgba(15,23,42,0.02), 0 4px 16px rgba(15,23,42,0.03)", zIndex: 50 }}>[m
         <div style={{ display: "flex", alignItems: "center", gap: "16px" }}>[m
           <span style={{ display: "inline-flex", alignItems: "center", gap: "8px" }}>[m
             <span style={{ width: "18px", height: "18px", borderRadius: "5px", background: "linear-gradient(135deg, #12B886 0%, #0A7A54 100%)", boxShadow: "0 1px 3px rgba(10,122,84,0.4), inset 0 1px 0 rgba(255,255,255,0.25)", display: "inline-block" }} />[m
             <span style={{ fontSize: "13px", fontWeight: 800, color: "#06281C", letterSpacing: "0.14em", textTransform: "uppercase", fontFamily: syne }}>Zero To Secure</span>[m
           </span>[m
[31m-          <div style={{ display: "flex", gap: "2px", background: "rgba(15,23,42,0.04)", borderRadius: "10px", padding: "3px", marginLeft: "12px", border: `1px solid rgba(15,23,42,0.04)` }}>[m
[31m-            {TABS.map(t => ([m
[31m-              <button key={t} onClick={() => setView(t)} style={{ padding: "5px 15px", background: view === t ? "#FFFFFF" : "transparent", border: "none", borderRadius: "7px", color: view === t ? "#0B1220" : "#8A97A8", fontSize: "11px", fontWeight: 700, cursor: "pointer", letterSpacing: "0.06em", textTransform: "uppercase", fontFamily: syne, boxShadow: view === t ? "0 1px 2px rgba(15,23,42,0.08), 0 2px 6px rgba(15,23,42,0.06)" : "none" }}>{t}</button>[m
[31m-            ))}[m
[31m-          </div>[m
[32m+[m[32m          {!isMobile && ([m
[32m+[m[32m            <div style={{ display: "flex", gap: "2px", background: "rgba(15,23,42,0.04)", borderRadius: "10px", padding: "3px", marginLeft: "12px", border: `1px solid rgba(15,23,42,0.04)` }}>[m
[32m+[m[32m              {TABS.map(t => ([m
[32m+[m[32m                <button key={t} onClick={() => setView(t)} style={{ padding: "5px 15px", background: view === t ? "#FFFFFF" : "transparent", border: "none", borderRadius: "7px", color: view === t ? "#0B1220" : "#8A97A8", fontSize: "11px", fontWeight: 700, cursor: "pointer", letterSpacing: "0.06em", textTransform: "uppercase", fontFamily: syne, boxShadow: view === t ? "0 1px 2px rgba(15,23,42,0.08), 0 2px 6px rgba(15,23,42,0.06)" : "none" }}>{t}</button>[m
[32m+[m[32m              ))}[m
[32m+[m[32m            </div>[m
[32m+[m[32m          )}[m
         </div>[m
       </div>[m
[31m-      {view === "mission" && <MissionView creators={creators} shorts={shorts} onNavigate={setView} />}[m
[31m-      {view === "creators" && <CreatorsView creators={creators} setCreators={setCreators} />}[m
[31m-      {view === "studio" && <StudioView shorts={shorts} setShorts={setShorts} />}[m
[31m-      {view === "seo" && <SeoView articles={articles} setArticles={setArticles} />}[m
[31m-      {view === "agents" && <AgentsView />}[m
[31m-      {view === "ops" && <OpsView />}[m
[32m+[m[32m      {view === "mission" && <MissionView creators={creators} shorts={shorts} onNavigate={setView} isMobile={isMobile} />}[m
[32m+[m[32m      {view === "creators" && <CreatorsView creators={creators} setCreators={setCreators} isMobile={isMobile} />}[m
[32m+[m[32m      {view === "studio" && <StudioView shorts={shorts} setShorts={setShorts} isMobile={isMobile} />}[m
[32m+[m[32m      {view === "seo" && <SeoView articles={articles} setArticles={setArticles} isMobile={isMobile} />}[m
[32m+[m[32m      {view === "agents" && <AgentsView isMobile={isMobile} />}[m
[32m+[m[32m      {view === "ops" && <OpsView isMobile={isMobile} />}[m
[32m+[m[32m      {isMobile && <BottomNav view={view} setView={setView} tabs={TABS} />}[m
     </div>[m
   );[m
 }[m
